# Note: If you see "npm WARN config global `--global`, `--local` are deprecated"
# This is a harmless warning from npm's internal configuration system.
# It does not affect the build process and can be safely ignored.

